{
  "name": "@apika_luca/mocorgo",
  "version": "1.1.0",
  "description": "A physics library in typescript",
  "main": "index.js",
  "scripts": {
    "rm:build": "rm -rf dist",
    "build:node": "tsc --build --verbose -p tsconfig.json && ef-tspm -v",
    "build:browser": "tsc --build --verbose -p tsconfig.browser.json && ef-tspm -v",
    "build": "yarn rm:build; yarn build:node && yarn build:browser",
    "prepublish": "yarn build"
  },
  "repository": "https://github.com/Brayan-724/mocorgo",
  "author": "Apika Luca",
  "license": "GNU GPLv3",
  "private": false,
  "devDependencies": {
    "@ef-carbon/tspm": "^2.2.5",
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "typescript": "^4.6.3"
  }
}
